apiVersion: v1
kind: Service
metadata:
  name: rest-api-service
  namespace: production
spec:
  selector:
    app: rest-api
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000